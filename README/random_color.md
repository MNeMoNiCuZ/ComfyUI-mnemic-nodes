# 🎲 Random Color

Generates a random RGB color.

-   **Description**: Generates a random color with both hex and RGB outputs.
-   **Inputs**:
    -   `seed` (optional): Seed for random number generator. Use -1 for random results, or set a specific value for reproducibility.
-   **Outputs**:
    -   `hex_color`: Color in hex format (e.g., `#FF5733`).
    -   `red`: Red channel value (0-255).
    -   `green`: Green channel value (0-255).
    -   `blue`: Blue channel value (0-255).

**Use Case**: Generate random colors for image generation, backgrounds, or any workflow that needs color variety.
