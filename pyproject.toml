[project]
name = "comfyui-mnemic-nodes"
description = "Fixed a bug in wildcard resolution nodes where incorrect wildcard usage would leak out to upper levels of wildcard nesting. Removed unsupported groq model"
version = "2.2.2"
license = { file = "LICENSE" }
dependencies = ["configparser", "groq", "transformers", "torch", "tiktoken", "imageio", "tqdm", "piexif", "requests", "colorama", "opencv-python"]

[project.urls]
Repository = "https://github.com/MNeMoNiCuZ/ComfyUI-mnemic-nodes"
#  Used by Comfy Registry https://comfyregistry.org

[tool.comfy]
PublisherId = "mnemic"
DisplayName = "ComfyUI-mnemic-nodes"
Icon = ""
