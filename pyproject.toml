[project]
name = "comfyui-mnemic-nodes"
description = "Split the Text Image Pairs node into 2 for performance. One for single outputs and one for lists. Added count and force refresh functionality."
version = "1.4.3"
license = { file = "LICENSE" }
dependencies = ["configparser", "groq", "transformers", "torch", "tiktoken"]

[project.urls]
Repository = "https://github.com/MNeMoNiCuZ/ComfyUI-mnemic-nodes"
#  Used by Comfy Registry https://comfyregistry.org

[tool.comfy]
PublisherId = "mnemic"
DisplayName = "ComfyUI-mnemic-nodes"
Icon = ""
