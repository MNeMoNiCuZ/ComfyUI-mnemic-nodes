# ✏️ Literal String

A multi-line text field you can wire into anything that takes a `STRING`.
Useful for a prompt fragment, a path, or a snippet reused in several places.

## Inputs

- **value** — Any text. Newlines are kept as typed.

## Outputs

- **value** — The same text, passed straight through.
