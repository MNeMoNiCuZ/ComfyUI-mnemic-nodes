# ✏️ Literal Float

A single decimal number you can wire into anything that takes a `FLOAT`.
Useful when several nodes need to share one value, such as a denoise or a CFG
scale.

## Inputs

- **value** — Any decimal number.

## Outputs

- **value** — The same number, passed straight through.
