# ✏️ Literal Int

A single whole number you can wire into anything that takes an `INT`.
Useful when several nodes need to share one number (a step count, a size, a
count) so you only edit it in one place.

## Inputs

- **value** — Any whole number, positive or negative.

## Outputs

- **value** — The same number, passed straight through.
