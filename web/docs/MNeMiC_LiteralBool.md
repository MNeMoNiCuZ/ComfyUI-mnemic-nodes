# ✏️ Literal Bool

A single boolean value you can wire into anything that takes a `BOOLEAN`.
Useful as one switch that drives several nodes at once, so you flip one toggle
instead of five.

## Inputs

- **value** — The toggle itself.

## Outputs

- **value** — The same value, passed straight through.
